{
  "scripts": {
    "start": "cd backend && yarn install && yarn start && cd ../frontend && yarn install && yarn start"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "crypto-js": "^4.1.1",
    "dotenv": "^16.3.1",
    "jsonwebtoken": "^9.0.1",
    "os-browserify": "^0.3.0",
    "path-browserify": "^1.0.1",
    "react-redux": "^8.1.1",
    "react-stripe-checkout": "^2.6.3",
    "redux": "^4.2.1",
    "slugify": "^1.6.6"
  }
}
