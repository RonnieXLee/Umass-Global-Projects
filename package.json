{
  "name": "ecommerceapi",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "concurrently \"cd backend && npm install && ./node_modules/bower/bin/bower install && npm run build\" \"cd frontend && yarn install && yarn start\"",
    "build": "grunt dist",
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "bower":"^1.8.0",
    "grunt":"^1.0.1"
  }
}
